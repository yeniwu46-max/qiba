server {
    listen 80;
    server_name 47.102.108.137;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    access_log /www/wwwlogs/qiba.wuyeni.cn.log;
    error_log /www/wwwlogs/qiba.wuyeni.cn.error.log;
}

server {
    listen 80;
    server_name qiba.wuyeni.cn;

    location ^~ /.well-known/acme-challenge/ {
        root /www/wwwroot/letsencrypt;
        default_type text/plain;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name qiba.wuyeni.cn;

    ssl_certificate /etc/letsencrypt/live/qiba.wuyeni.cn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/qiba.wuyeni.cn/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    access_log /www/wwwlogs/qiba.wuyeni.cn.log;
    error_log /www/wwwlogs/qiba.wuyeni.cn.error.log;
}
